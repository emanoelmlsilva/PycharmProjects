
from math import sin,cos,tan,radians
#import math
ang = float(input('Informe o angulo: '))
seno = sin(radians(ang))
cosse =cos(radians(ang))
tang = tan(radians(ang))
print('O angulo de  {:.2f} tem seno {:.2f} o cosseno é {:.2f} e a tangente é {:.2f}'.format(ang,seno,cosse,tang))

