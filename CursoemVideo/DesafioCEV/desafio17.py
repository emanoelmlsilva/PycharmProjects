from math import sqrt,pow,hypot
catOP = float(input('Cateto oposto: '))
catAD = float(input('Cateto adjacente: '))
raiz = pow(catOP,2)+pow(catAD,2)
hp = sqrt(raiz)
"""
print('hipotnusa: {}'.format(hypot(catOP,catAD))
"""
print('hipotnusa: {}'.format(hp))

